name: 'emdb_analytics'
version: '1.0.0'
config-version: 2

profile: 'emdb_analytics'

model-paths: ['models']
test-paths: ['tests']
target-path: 'target'
clean-targets: ['target', 'dbt_packages']

models:
  emdb_analytics:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
